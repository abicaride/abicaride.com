---
import ProjectPage from '../../../components/pages/ProjectPage.astro';
import { getProjectStaticPaths, type ProjectRouteProps } from '../../../lib/projects';

export function getStaticPaths() {
  return getProjectStaticPaths('es');
}

const { project, alternatePath } = Astro.props as ProjectRouteProps;
---

<ProjectPage locale="es" project={project} alternatePath={alternatePath} />
