---
import WritingArticlePage from '../../../components/pages/WritingArticlePage.astro';
import { getWritingStaticPaths, type WritingRouteProps } from '../../../lib/writing';

export function getStaticPaths() {
  return getWritingStaticPaths('es');
}

const { article, alternatePath } = Astro.props as WritingRouteProps;
---

<WritingArticlePage locale="es" article={article} alternatePath={alternatePath} />
